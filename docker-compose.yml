services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: "production"
      PORT: "3000"
      MONGO_URL: "mongodb://mongo:27017/smart_reminders"
      HA_URL: "${HA_URL}"
      HA_TOKEN: "${HA_TOKEN}"
    depends_on:
      - mongo

  mongo:
    image: mongo:8
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
